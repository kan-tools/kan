# What THIS BUILD does with a workspace written by each released kan.
#
# Columns, tab-separated:  <tag>  <mode>  <outcome>  <why>
#
# THE `why` COLUMN STARTS WITH ITS OWN CONFIDENCE, either MEASURED or
# PREDICTED, and the workflow enforces the difference: if a cell actually
# EXECUTES (not a cache hit) while its row still says PREDICTED, that is an
# error with a one-line fix. Added by v0.12 because v0.9.2, v0.10.0 and
# v0.11.0 all sat at "PREDICTED at cut time -- confirmed or corrected at the
# next release" across two releases that had already confirmed them. Nothing
# failed when the confirmation was skipped, so a prediction was
# indistinguishable from a measurement and the table under-reported its own
# confidence. All six are now converted, with their run ids.
#
# `mode` is how the identity is resolved on both sides of the cell (#146
# part 3). The matrix used to drive `KAN_IDENTITY_FILE` in every cell, which
# is the one branch that short-circuits the other two -- so seed-rooting,
# added in v0.9, had never been run against an older workspace, and #146's
# defect lived where no cell could reach it.
#
#   identity-file  both sides name a dedicated key file  [ubuntu-latest]
#   seed           neither does; the reader takes the seed-rooting path
#                                                          [ubuntu-latest]
#   keychain       neither side names a key file AND the keychain is not
#                  disabled, so the secret goes to the OS keychain and the
#                  reader must find it there       [macos-latest, v0.12+]
#
# WHAT A GREEN `keychain` ROW DOES AND DOES NOT MEAN -- read this before
# treating one as coverage of #96.
#
# CORRECTED AFTER A COLD REVIEW, and the correction is the point: the first
# version of this paragraph claimed the axis covers "a secret filed in the OS
# keychain by one binary is found and used by a DIFFERENT, later binary". IT
# DID NOT. The cell's only reader action was `show --json`, and a read resolves
# no identity at all (`src/workspace.rs::open_read_only`, ADR-83) -- so every
# outcome was independent of whether the upgraded binary could reach the
# writer's secret. A workspace this build could read but could neither write to
# nor name its own DID in scored `ok`.
#
# The cell now runs `kan identity did` after the read and compares the resolved
# DID against the claims' author, so the retrieval the paragraph described is
# actually performed. Thirteen of the fourteen cells exercised the keychain on
# the writer side (v0.1.1 predates it and is recorded `keychain-unused`); the
# reader side is measured for the first time by this change, and the rows
# below were re-measured under it.
#
# WHAT A v0.12+ KEYCHAIN ROW MEASURES IS DIFFERENT IN KIND from a
# v0.7-v0.11 one, and the difference is REQ-3's doing. Through v0.11 the
# writer filed in the keychain BY DEFAULT, so those rows measure "the default
# put it there". From v0.12 the default is a plaintext 0600 secret and the
# keychain is an opt-in, so the cell runs `kan identity protect` on the
# writer's workspace when the writer has that subcommand -- a v0.12+ row
# measures "protect put it there". Without that step the axis stays
# `keychain-unused` for every future version: green, and exercising nothing.
# This is the SECOND axis a v0.12 requirement quietly retired -- REQ-2 did
# the same to identity-file by making a named-but-absent path an error
# instead of a mint. Both are written down here rather than left to be
# rediscovered a third time.
#
# It does NOT cover the ACL dimension, and #96 DID NOT REPRODUCE on the hosted
# runner. On a developer's Mac a keychain entry is authorised to the exact
# binary that created it, so a rebuilt or upgraded kan waits on an
# authorization prompt -- that is #96, and it is the failure this whole
# milestone is routed around. The runner did not raise it. The `keychain-modal`
# outcome exists for it and no cell has yet produced one. So a green keychain
# row must not be read as "#96 cannot happen"; it means the storage path works
# when the ACL does not intervene. Stated as a bounded limit rather than left
# for someone to infer from a green check.
#
# (Historical note, now resolved:)
# THE `keychain` MODE HAD NO ROWS AT FIRST, DELIBERATELY. Its outcomes are not
# knowable without running it, and inventing them would be exactly the
# prediction-as-measurement problem the confidence token above exists to
# stop. The first macOS run will fail with "has no committed expectation"
# for each cell and print the actual outcome as a notice; those actuals get
# committed, with their run id, in a following commit. That is the designed
# loop, not a broken gate.
#
# Expect at least some of them to come back `keychain-modal`: writer and
# reader are different binaries, which is precisely the ACL mismatch of #96.
# Recording that as an outcome is the point -- it turns #96 from an anecdote
# into a migration fact with a row, and gives v0.12's at-rest flip something
# to be measured against rather than asserted.
#
# The mode implies its platform, so (tag, mode) still identifies a row.
# Linux has no Secret Service, so `identity-file` and `seed` BOTH degrade to
# a plaintext key on disk there -- which is why the OS-keychain plane, where
# #90, #96, #107, #170 and #180 all lived, had never been executed by any
# cell on any version before v0.12.
#
# Generated by `scripts/run-migration-cell.sh`, enforced by
# `.github/workflows/migration-matrix.yml`. A tag with no row is an error
# rather than a pass: what this build does with an older workspace is a
# decision someone makes, never a default.
#
# The third column is the point of keeping this file rather than just a
# pass/fail gate. Not every break is a defect -- some are the known
# consequence of a change that was made deliberately -- and a table that
# cannot tell those apart is one that gets edited to green rather than read.
# The `why` is the bookkeeping: which feature change explains this cell.
#
# HISTORY WORTH KEEPING: v0.9.0-beta.1 and v0.9.1-beta.1 had no rows here at
# all until #146. The matrix therefore failed on the v0.9.1 tag push
# (run 30582674101, 2026-07-30) with "v0.9.0-beta.1 has no committed
# expectation" -- a release shipped with this table red and it went unread for
# two days. The gate works; nobody was looking at it. The actual outcome in
# that run was `ok`, so nothing was lost, which is exactly why it was easy to
# miss.
#
# Outcomes:
#   ok                the old workspace reads correctly under this build
#   unbuildable       the tag does not compile with the current toolchain
#   unwritable        the tag builds but could not create a workspace
#   claims-lost       this build sees fewer claims than the old binary wrote
#   identity-changed  claims present but excluded by trust (the #90 shape)
#   claims-altered    the right NUMBER of claims, but not the same ones: the
#                     CID set moved, so content changed under a migration that
#                     reported success. Until v0.12 this script compared two
#                     integers and nothing else, so a migration preserving
#                     three claims and corrupting all three bodies scored `ok`.
#                     kan is content-addressed, which makes the strong check
#                     cheap -- equal CIDs mean byte-identical claims.
#   keychain-unused   the cell asked for the keychain and did not get it -- the
#                     writer predates keychain support, the runner's keychain
#                     was unreachable, or the cell's own `identity protect`
#                     opt-in failed -- so the workspace is plaintext
#                     and this cell did NOT exercise the keychain plane.
#                     Recorded rather than scored `ok`, so the table shows
#                     which versions that plane actually covers. Added after
#                     the mode's FIRST run returned `ok` for all fourteen tags
#                     with no evidence the keychain was involved beyond the
#                     absence of a fallback warning.
#   keychain-modal  the read never returned. A keychain entry is ACL'd to the
#                     binary that created it, so the reader waits on an
#                     authorization prompt no runner can answer (#96). A hang
#                     is neither a pass nor a crash and gets its own word.
#
# `ok` is a GUARANTEE for anything a user could plausibly still be running:
# kan owns this on-disk format, and an upgrade must not lose a log. A row
# moving OFF `ok` stops the release. A row that is already something else
# carries its reason here, and changing that reason is a decision, not an
# edit.
#
# THE ACTUAL OUTCOME IS ALWAYS REPORTED, whether or not it matches. The
# workflow emits every cell as a notice and puts the run's results in the job
# summary, so the table is checked against reality rather than standing in
# for it.
#
# Populated from a real CI run on ubuntu-latest -- deliberately not from a
# developer laptop. macOS keeps early versions' keys in the OS keychain,
# where Linux CI falls back to a plaintext `.kan/identity`, so the two
# platforms genuinely disagree about some cells and only the platform the
# matrix runs on is ground truth.
#
# --- rows -------------------------------------------------------------------
# Populated from run 30567804137 on ubuntu-latest (2026-07-30), against the
# build at v0.9's durability + encryption-key work.
#
# Every cell is `ok`, and the reason is the same one nine times over: no claim
# format change kan has ever shipped was a breaking one. SPEC 7.1 froze
# existing fields, made new ones additive and optional, and made an
# unrecognized claim kind a preserved opaque claim rather than a rejection.
# The per-tag notes below record which crossing each cell actually exercises,
# so that if one ever moves off `ok` it is clear what changed underneath it.
v0.1.1-beta.1	identity-file	ok	MEASURED -- first release. Predates KAN_IDENTITY_FILE entirely, so this cell exercises the plaintext .kan/identity path and proves the oldest on-disk shape still reads.
v0.2.0-beta.1	identity-file	ok	MEASURED -- added KAN_AGENT (removed later by ADR-48). Unset here, so AuthorId.agent is None on both sides -- see the KAN_AGENT limitation note below, which this cell does NOT cover.
v0.3.0-beta.1	identity-file	ok	MEASURED -- retract/reject split. Both are claim kinds this build still understands, so nothing is opaque.
v0.4.0-beta.1	identity-file	ok	MEASURED -- index rebuild skip (#26) and the subject-naming nudge. Index is disposable and rebuilt on open, so it cannot carry a migration hazard.
v0.5.0-beta.1	identity-file	ok	MEASURED -- transport trait + LocalOnly. Pure additive refactor; no on-disk change at all.
v0.6.0-beta.1	identity-file	ok	MEASURED -- gitTree publish and the schema-evolution contract (ADR-44). First release whose claims could contain a Publication body, which this build reads natively.
v0.7.0-beta.1	identity-file	ok	MEASURED -- the correctness release. Claims gain recorded_at; earlier claims have none, which is why the field is Option rather than required.
v0.7.1-beta.1	identity-file	ok	MEASURED -- wave 1 ergonomics and the phrase-off-argv fix. CLI-surface only; the on-disk shape is byte-identical to v0.7.0.
v0.8.0-beta.1	identity-file	ok	MEASURED -- reader, trust surface, multi-role. Adds .kan/overlay and .kan/roles beside the log rather than changing it, so a v0.8 workspace is a v0.7 workspace plus files this build creates anyway.
#
# --- what this matrix does NOT cover ----------------------------------------
#
# KAN_AGENT. Versions v0.2.0 through v0.6.0 hashed the KAN_AGENT environment
# variable into AuthorId.agent. A user who SET it has claims whose author is
# {did, agent: Some(..)}, and this build always computes agent: None -- so
# those claims are authored by an AuthorId this build never constructs, and a
# Solo read cannot see them. That is a REAL break, and it is the one ADR-48
# removed KAN_AGENT over: kan's own shipped .mcp.json set it.
#
# The cell above does not set KAN_AGENT, so it does not exercise this. That is
# a deliberate limit rather than an oversight: reproducing it needs a second
# axis (with/without the variable). Recorded here so the gap is visible in the
# table rather than discovered by someone whose claims vanished.
#
# CORRECTION (2026-07-30, while building v0.9 REQ-8): an earlier version of
# this note said `kan identity adopt` was the fix. It is not, and the reason
# is worth keeping. Adopt repoints a workspace at a different *key*; the
# KAN_AGENT case has the RIGHT key -- the DID matches exactly -- and differs
# only in AuthorId.agent. So adopt cannot help, and neither can `--trust`,
# since a trust base names AuthorIds and agent is part of one. The real fix is
# read-side: matching an author by DID irrespective of agent, which no surface
# does today. Filed as its own issue rather than smuggled into a recovery
# command it does not fit.
#
# Since v0.8 the failure is at least LOUD: excluded_by_trust reports the
# hidden claims instead of the read looking empty (ADR-57).
v0.1.1-beta.1	seed	ok	MEASURED -- the writer leaves a plaintext .kan/identity (no Secret Service on CI), so this build finds a key file, reads the workspace as non-fresh, and loads that key rather than seed-rooting over it. Measured in run 30707872913, the first run of this axis.
v0.2.0-beta.1	seed	ok	MEASURED -- the writer leaves a plaintext .kan/identity (no Secret Service on CI), so this build finds a key file, reads the workspace as non-fresh, and loads that key rather than seed-rooting over it. Measured in run 30707872913, the first run of this axis.
v0.3.0-beta.1	seed	ok	MEASURED -- the writer leaves a plaintext .kan/identity (no Secret Service on CI), so this build finds a key file, reads the workspace as non-fresh, and loads that key rather than seed-rooting over it. Measured in run 30707872913, the first run of this axis.
v0.4.0-beta.1	seed	ok	MEASURED -- the writer leaves a plaintext .kan/identity (no Secret Service on CI), so this build finds a key file, reads the workspace as non-fresh, and loads that key rather than seed-rooting over it. Measured in run 30707872913, the first run of this axis.
v0.5.0-beta.1	seed	ok	MEASURED -- the writer leaves a plaintext .kan/identity (no Secret Service on CI), so this build finds a key file, reads the workspace as non-fresh, and loads that key rather than seed-rooting over it. Measured in run 30707872913, the first run of this axis.
v0.6.0-beta.1	seed	ok	MEASURED -- the writer leaves a plaintext .kan/identity (no Secret Service on CI), so this build finds a key file, reads the workspace as non-fresh, and loads that key rather than seed-rooting over it. Measured in run 30707872913, the first run of this axis.
v0.7.0-beta.1	seed	ok	MEASURED -- the writer leaves a plaintext .kan/identity (no Secret Service on CI), so this build finds a key file, reads the workspace as non-fresh, and loads that key rather than seed-rooting over it. Measured in run 30707872913, the first run of this axis.
v0.7.1-beta.1	seed	ok	MEASURED -- the writer leaves a plaintext .kan/identity (no Secret Service on CI), so this build finds a key file, reads the workspace as non-fresh, and loads that key rather than seed-rooting over it. Measured in run 30707872913, the first run of this axis.
v0.8.0-beta.1	seed	ok	MEASURED -- the writer leaves a plaintext .kan/identity (no Secret Service on CI), so this build finds a key file, reads the workspace as non-fresh, and loads that key rather than seed-rooting over it. Measured in run 30707872913, the first run of this axis.
v0.9.0-beta.1	identity-file	ok	MEASURED -- first seed-rooting release, but KAN_IDENTITY_FILE short-circuits that path, so it writes a key file exactly as every version before it did. Measured in run 30707872913.
v0.9.1-beta.1	identity-file	ok	MEASURED -- bulk read (ADR-71/72); no on-disk change from v0.9.0. Measured in run 30707872913.
v0.9.0-beta.1	seed	ok	MEASURED -- the cell this axis was added for: the writer genuinely seed-roots, creating .kan/seed, and this build derives the same DID from that seed rather than minting over it. Measured in run 30707872913.
v0.9.1-beta.1	seed	ok	MEASURED -- as v0.9.0 -- seed-rooted writer, seed-derived reader, same DID. Measured in run 30707872913.
v0.9.2-beta.1	identity-file	ok	MEASURED -- in run 31038419269 (v0.11.0-beta.1 tag push, reader v0.11.0) and again in 31151700728 (reader: post-REQ-1/REQ-2 main). Both cache misses, so both are executions rather than replays. Was PREDICTED at cut time; converted by v0.12's matrix work.
v0.9.2-beta.1	seed	ok	MEASURED -- in run 31038419269 (v0.11.0-beta.1 tag push, reader v0.11.0) and again in 31151700728 (reader: post-REQ-1/REQ-2 main). Both cache misses. Was PREDICTED at cut time; converted by v0.12's matrix work.
v0.10.0-beta.1	identity-file	ok	MEASURED -- in run 31038419269 (v0.11.0-beta.1 tag push) and again in 31151700728. Was PREDICTED at cut time; converted by v0.12's matrix work.
v0.10.0-beta.1	seed	ok	MEASURED -- in run 31038419269 (v0.11.0-beta.1 tag push) and again in 31151700728. Was PREDICTED at cut time; converted by v0.12's matrix work.
v0.11.0-beta.1	identity-file	ok	MEASURED -- in run 31151700728 (reader: post-REQ-1/REQ-2 main), a cache miss. v0.11.0 could not be its own historical writer at its own tag push, so this is its first measurement. Was PREDICTED at cut time.
v0.11.0-beta.1	seed	ok	MEASURED -- in run 31151700728 (reader: post-REQ-1/REQ-2 main), a cache miss. First measurement, for the same reason. Was PREDICTED at cut time.
v0.1.1-beta.1	keychain	keychain-unused	MEASURED in run 31268632749 (macos-latest, reader: the cell that now resolves an identity). Predates OS-keychain support, so the writer left a plaintext secret and this cell does not exercise the keychain plane.
v0.2.0-beta.1	keychain	identity-unresolvable	MEASURED in run 31268632749 (macos-latest, reader: the cell that now resolves an identity). The writer filed its key under an account derived from the canonicalized .kan/identity PATH and left no pointer file (the scheme v0.7 REQ-5 replaced). This build's keychain_identity requires .kan/identity-id, so it CANNOT retrieve that secret by any route: the claims fold, and the workspace can neither be written to nor name its own DID. Previously scored `ok`, because the cell's only reader action was a read and a read resolves no identity.
v0.3.0-beta.1	keychain	identity-unresolvable	MEASURED in run 31268632749 (macos-latest, reader: the cell that now resolves an identity). The writer filed its key under an account derived from the canonicalized .kan/identity PATH and left no pointer file (the scheme v0.7 REQ-5 replaced). This build's keychain_identity requires .kan/identity-id, so it CANNOT retrieve that secret by any route: the claims fold, and the workspace can neither be written to nor name its own DID. Previously scored `ok`, because the cell's only reader action was a read and a read resolves no identity.
v0.4.0-beta.1	keychain	identity-unresolvable	MEASURED in run 31268632749 (macos-latest, reader: the cell that now resolves an identity). The writer filed its key under an account derived from the canonicalized .kan/identity PATH and left no pointer file (the scheme v0.7 REQ-5 replaced). This build's keychain_identity requires .kan/identity-id, so it CANNOT retrieve that secret by any route: the claims fold, and the workspace can neither be written to nor name its own DID. Previously scored `ok`, because the cell's only reader action was a read and a read resolves no identity.
v0.5.0-beta.1	keychain	identity-unresolvable	MEASURED in run 31268632749 (macos-latest, reader: the cell that now resolves an identity). The writer filed its key under an account derived from the canonicalized .kan/identity PATH and left no pointer file (the scheme v0.7 REQ-5 replaced). This build's keychain_identity requires .kan/identity-id, so it CANNOT retrieve that secret by any route: the claims fold, and the workspace can neither be written to nor name its own DID. Previously scored `ok`, because the cell's only reader action was a read and a read resolves no identity.
v0.6.0-beta.1	keychain	identity-unresolvable	MEASURED in run 31268632749 (macos-latest, reader: the cell that now resolves an identity). The writer filed its key under an account derived from the canonicalized .kan/identity PATH and left no pointer file (the scheme v0.7 REQ-5 replaced). This build's keychain_identity requires .kan/identity-id, so it CANNOT retrieve that secret by any route: the claims fold, and the workspace can neither be written to nor name its own DID. Previously scored `ok`, because the cell's only reader action was a read and a read resolves no identity.
v0.7.0-beta.1	keychain	keychain-modal	MEASURED in run 31268632749 (macos-latest, reader: the cell that now resolves an identity). #96 REPRODUCED IN CI FOR THE FIRST TIME. The writer files the secret in the keychain successfully; the reader is a DIFFERENT binary, so the entry's trusted-application ACL does not match and macOS raises an authorization prompt no runner can answer. Sequence in the log: keychain confirmed in use -> read succeeds (resolving no identity) -> resolving an identity does not return in 120s. This is the at-rest posture v0.12 REQ-3 moves the default away from; `kan identity unprotect` is the exit for a workspace already in this state, and the keychain-recovery mode measures whether that exit works.
v0.7.1-beta.1	keychain	keychain-modal	MEASURED in run 31268632749 (macos-latest, reader: the cell that now resolves an identity). #96 REPRODUCED IN CI FOR THE FIRST TIME. The writer files the secret in the keychain successfully; the reader is a DIFFERENT binary, so the entry's trusted-application ACL does not match and macOS raises an authorization prompt no runner can answer. Sequence in the log: keychain confirmed in use -> read succeeds (resolving no identity) -> resolving an identity does not return in 120s. This is the at-rest posture v0.12 REQ-3 moves the default away from; `kan identity unprotect` is the exit for a workspace already in this state, and the keychain-recovery mode measures whether that exit works.
v0.8.0-beta.1	keychain	keychain-modal	MEASURED in run 31268632749 (macos-latest, reader: the cell that now resolves an identity). #96 REPRODUCED IN CI FOR THE FIRST TIME. The writer files the secret in the keychain successfully; the reader is a DIFFERENT binary, so the entry's trusted-application ACL does not match and macOS raises an authorization prompt no runner can answer. Sequence in the log: keychain confirmed in use -> read succeeds (resolving no identity) -> resolving an identity does not return in 120s. This is the at-rest posture v0.12 REQ-3 moves the default away from; `kan identity unprotect` is the exit for a workspace already in this state, and the keychain-recovery mode measures whether that exit works.
v0.9.0-beta.1	keychain	keychain-modal	MEASURED in run 31268632749 (macos-latest, reader: the cell that now resolves an identity). #96 REPRODUCED IN CI FOR THE FIRST TIME. The writer files the secret in the keychain successfully; the reader is a DIFFERENT binary, so the entry's trusted-application ACL does not match and macOS raises an authorization prompt no runner can answer. Sequence in the log: keychain confirmed in use -> read succeeds (resolving no identity) -> resolving an identity does not return in 120s. This is the at-rest posture v0.12 REQ-3 moves the default away from; `kan identity unprotect` is the exit for a workspace already in this state, and the keychain-recovery mode measures whether that exit works.
v0.9.1-beta.1	keychain	keychain-modal	MEASURED in run 31268632749 (macos-latest, reader: the cell that now resolves an identity). #96 REPRODUCED IN CI FOR THE FIRST TIME. The writer files the secret in the keychain successfully; the reader is a DIFFERENT binary, so the entry's trusted-application ACL does not match and macOS raises an authorization prompt no runner can answer. Sequence in the log: keychain confirmed in use -> read succeeds (resolving no identity) -> resolving an identity does not return in 120s. This is the at-rest posture v0.12 REQ-3 moves the default away from; `kan identity unprotect` is the exit for a workspace already in this state, and the keychain-recovery mode measures whether that exit works.
v0.9.2-beta.1	keychain	keychain-modal	MEASURED in run 31268632749 (macos-latest, reader: the cell that now resolves an identity). #96 REPRODUCED IN CI FOR THE FIRST TIME. The writer files the secret in the keychain successfully; the reader is a DIFFERENT binary, so the entry's trusted-application ACL does not match and macOS raises an authorization prompt no runner can answer. Sequence in the log: keychain confirmed in use -> read succeeds (resolving no identity) -> resolving an identity does not return in 120s. This is the at-rest posture v0.12 REQ-3 moves the default away from; `kan identity unprotect` is the exit for a workspace already in this state, and the keychain-recovery mode measures whether that exit works.
v0.10.0-beta.1	keychain	keychain-modal	MEASURED in run 31268632749 (macos-latest, reader: the cell that now resolves an identity). #96 REPRODUCED IN CI FOR THE FIRST TIME. The writer files the secret in the keychain successfully; the reader is a DIFFERENT binary, so the entry's trusted-application ACL does not match and macOS raises an authorization prompt no runner can answer. Sequence in the log: keychain confirmed in use -> read succeeds (resolving no identity) -> resolving an identity does not return in 120s. This is the at-rest posture v0.12 REQ-3 moves the default away from; `kan identity unprotect` is the exit for a workspace already in this state, and the keychain-recovery mode measures whether that exit works.
v0.11.0-beta.1	keychain	keychain-modal	MEASURED in run 31268632749 (macos-latest, reader: the cell that now resolves an identity). #96 REPRODUCED IN CI FOR THE FIRST TIME. The writer files the secret in the keychain successfully; the reader is a DIFFERENT binary, so the entry's trusted-application ACL does not match and macOS raises an authorization prompt no runner can answer. Sequence in the log: keychain confirmed in use -> read succeeds (resolving no identity) -> resolving an identity does not return in 120s. This is the at-rest posture v0.12 REQ-3 moves the default away from; `kan identity unprotect` is the exit for a workspace already in this state, and the keychain-recovery mode measures whether that exit works.
v0.1.1-beta.1	keychain-recovery	keychain-unused	MEASURED in run 31287243890 (macos-latest, with `kan identity unprotect` present). Predates keychain support, so there is no block to recover from.
v0.2.0-beta.1	keychain-recovery	fix-route-failed	MEASURED in run 31287243890 (macos-latest, with `kan identity unprotect` present). NO REMEDY EXISTS FOR THESE VERSIONS, on any platform, interactive or not. They filed the secret under an account derived from the canonicalized .kan/identity PATH and left NO pointer file, so `unprotect` has nothing to look up: at_rest sees no pointer, plan_unprotect reports no identity to unprotect, and it errors. Accepted and recorded rather than fixed (decided with Maxine): reconstructing the legacy path-derived account would be new scope inside REQ-3.3, and these are pre-v0.7.0 betas. The route out is `kan identity adopt --key <file>` with a key extracted by hand -- and kan#190 records that a recovery phrase cannot produce that file.
v0.3.0-beta.1	keychain-recovery	fix-route-failed	MEASURED in run 31287243890 (macos-latest, with `kan identity unprotect` present). NO REMEDY EXISTS FOR THESE VERSIONS, on any platform, interactive or not. They filed the secret under an account derived from the canonicalized .kan/identity PATH and left NO pointer file, so `unprotect` has nothing to look up: at_rest sees no pointer, plan_unprotect reports no identity to unprotect, and it errors. Accepted and recorded rather than fixed (decided with Maxine): reconstructing the legacy path-derived account would be new scope inside REQ-3.3, and these are pre-v0.7.0 betas. The route out is `kan identity adopt --key <file>` with a key extracted by hand -- and kan#190 records that a recovery phrase cannot produce that file.
v0.4.0-beta.1	keychain-recovery	fix-route-failed	MEASURED in run 31287243890 (macos-latest, with `kan identity unprotect` present). NO REMEDY EXISTS FOR THESE VERSIONS, on any platform, interactive or not. They filed the secret under an account derived from the canonicalized .kan/identity PATH and left NO pointer file, so `unprotect` has nothing to look up: at_rest sees no pointer, plan_unprotect reports no identity to unprotect, and it errors. Accepted and recorded rather than fixed (decided with Maxine): reconstructing the legacy path-derived account would be new scope inside REQ-3.3, and these are pre-v0.7.0 betas. The route out is `kan identity adopt --key <file>` with a key extracted by hand -- and kan#190 records that a recovery phrase cannot produce that file.
v0.5.0-beta.1	keychain-recovery	fix-route-failed	MEASURED in run 31287243890 (macos-latest, with `kan identity unprotect` present). NO REMEDY EXISTS FOR THESE VERSIONS, on any platform, interactive or not. They filed the secret under an account derived from the canonicalized .kan/identity PATH and left NO pointer file, so `unprotect` has nothing to look up: at_rest sees no pointer, plan_unprotect reports no identity to unprotect, and it errors. Accepted and recorded rather than fixed (decided with Maxine): reconstructing the legacy path-derived account would be new scope inside REQ-3.3, and these are pre-v0.7.0 betas. The route out is `kan identity adopt --key <file>` with a key extracted by hand -- and kan#190 records that a recovery phrase cannot produce that file.
v0.6.0-beta.1	keychain-recovery	fix-route-failed	MEASURED in run 31287243890 (macos-latest, with `kan identity unprotect` present). NO REMEDY EXISTS FOR THESE VERSIONS, on any platform, interactive or not. They filed the secret under an account derived from the canonicalized .kan/identity PATH and left NO pointer file, so `unprotect` has nothing to look up: at_rest sees no pointer, plan_unprotect reports no identity to unprotect, and it errors. Accepted and recorded rather than fixed (decided with Maxine): reconstructing the legacy path-derived account would be new scope inside REQ-3.3, and these are pre-v0.7.0 betas. The route out is `kan identity adopt --key <file>` with a key extracted by hand -- and kan#190 records that a recovery phrase cannot produce that file.
v0.7.0-beta.1	keychain-recovery	fix-route-blocked	MEASURED in run 31287243890 (macos-latest, with `kan identity unprotect` present). THE REMEDY IS BLOCKED BY THE CONDITION IT REMEDIES, and this is the design working rather than a defect. `unprotect` must READ the keychain to move the secret out of it, and reading is what #96 prevents -- so on a headless runner it blocks, exactly as `.design/identity-at-rest.md` says it will: unprotect is an interactive command by design, and a keychain prompt there is one a human is present to answer. What this row establishes is the qualifier the design doc omitted: for CI, `day`, MCP and agents there is NO remedy at all. A human at a Mac terminal answers the prompt and it works; nothing headless does.
v0.7.1-beta.1	keychain-recovery	fix-route-blocked	MEASURED in run 31287243890 (macos-latest, with `kan identity unprotect` present). THE REMEDY IS BLOCKED BY THE CONDITION IT REMEDIES, and this is the design working rather than a defect. `unprotect` must READ the keychain to move the secret out of it, and reading is what #96 prevents -- so on a headless runner it blocks, exactly as `.design/identity-at-rest.md` says it will: unprotect is an interactive command by design, and a keychain prompt there is one a human is present to answer. What this row establishes is the qualifier the design doc omitted: for CI, `day`, MCP and agents there is NO remedy at all. A human at a Mac terminal answers the prompt and it works; nothing headless does.
v0.8.0-beta.1	keychain-recovery	fix-route-blocked	MEASURED in run 31287243890 (macos-latest, with `kan identity unprotect` present). THE REMEDY IS BLOCKED BY THE CONDITION IT REMEDIES, and this is the design working rather than a defect. `unprotect` must READ the keychain to move the secret out of it, and reading is what #96 prevents -- so on a headless runner it blocks, exactly as `.design/identity-at-rest.md` says it will: unprotect is an interactive command by design, and a keychain prompt there is one a human is present to answer. What this row establishes is the qualifier the design doc omitted: for CI, `day`, MCP and agents there is NO remedy at all. A human at a Mac terminal answers the prompt and it works; nothing headless does.
v0.9.0-beta.1	keychain-recovery	fix-route-blocked	MEASURED in run 31287243890 (macos-latest, with `kan identity unprotect` present). THE REMEDY IS BLOCKED BY THE CONDITION IT REMEDIES, and this is the design working rather than a defect. `unprotect` must READ the keychain to move the secret out of it, and reading is what #96 prevents -- so on a headless runner it blocks, exactly as `.design/identity-at-rest.md` says it will: unprotect is an interactive command by design, and a keychain prompt there is one a human is present to answer. What this row establishes is the qualifier the design doc omitted: for CI, `day`, MCP and agents there is NO remedy at all. A human at a Mac terminal answers the prompt and it works; nothing headless does.
v0.9.1-beta.1	keychain-recovery	fix-route-blocked	MEASURED in run 31287243890 (macos-latest, with `kan identity unprotect` present). THE REMEDY IS BLOCKED BY THE CONDITION IT REMEDIES, and this is the design working rather than a defect. `unprotect` must READ the keychain to move the secret out of it, and reading is what #96 prevents -- so on a headless runner it blocks, exactly as `.design/identity-at-rest.md` says it will: unprotect is an interactive command by design, and a keychain prompt there is one a human is present to answer. What this row establishes is the qualifier the design doc omitted: for CI, `day`, MCP and agents there is NO remedy at all. A human at a Mac terminal answers the prompt and it works; nothing headless does.
v0.9.2-beta.1	keychain-recovery	fix-route-blocked	MEASURED in run 31287243890 (macos-latest, with `kan identity unprotect` present). THE REMEDY IS BLOCKED BY THE CONDITION IT REMEDIES, and this is the design working rather than a defect. `unprotect` must READ the keychain to move the secret out of it, and reading is what #96 prevents -- so on a headless runner it blocks, exactly as `.design/identity-at-rest.md` says it will: unprotect is an interactive command by design, and a keychain prompt there is one a human is present to answer. What this row establishes is the qualifier the design doc omitted: for CI, `day`, MCP and agents there is NO remedy at all. A human at a Mac terminal answers the prompt and it works; nothing headless does.
v0.10.0-beta.1	keychain-recovery	fix-route-blocked	MEASURED in run 31287243890 (macos-latest, with `kan identity unprotect` present). THE REMEDY IS BLOCKED BY THE CONDITION IT REMEDIES, and this is the design working rather than a defect. `unprotect` must READ the keychain to move the secret out of it, and reading is what #96 prevents -- so on a headless runner it blocks, exactly as `.design/identity-at-rest.md` says it will: unprotect is an interactive command by design, and a keychain prompt there is one a human is present to answer. What this row establishes is the qualifier the design doc omitted: for CI, `day`, MCP and agents there is NO remedy at all. A human at a Mac terminal answers the prompt and it works; nothing headless does.
v0.11.0-beta.1	keychain-recovery	fix-route-blocked	MEASURED in run 31287243890 (macos-latest, with `kan identity unprotect` present). THE REMEDY IS BLOCKED BY THE CONDITION IT REMEDIES, and this is the design working rather than a defect. `unprotect` must READ the keychain to move the secret out of it, and reading is what #96 prevents -- so on a headless runner it blocks, exactly as `.design/identity-at-rest.md` says it will: unprotect is an interactive command by design, and a keychain prompt there is one a human is present to answer. What this row establishes is the qualifier the design doc omitted: for CI, `day`, MCP and agents there is NO remedy at all. A human at a Mac terminal answers the prompt and it works; nothing headless does.
v0.12.0-beta.1	identity-file	ok	MEASURED in run 31439573855 (ubuntu-latest; the beta.2 harness-hash change invalidated every cache entry, so this cell executed for the first time -- beta.1 is excluded as a writer at its own tag push). As predicted: the harness seeds the key deliberately (route 2, `identity role add`, since REQ-2 forbids minting at a named-but-absent path) and the reader follows the named file.
v0.12.0-beta.1	seed	ok	MEASURED in run 31439573855 (ubuntu-latest, same first execution as the identity-file row). As predicted: .kan/seed is what a fresh v0.12 workspace produces, and the default path keeps reading clean.
v0.12.0-beta.1	keychain	keychain-modal	MEASURED in run 31444789994 (tag push v0.12.0-beta.2, macos-latest) -- AND THE MEASUREMENT IS UNSTABLE, kan#205: run 31439573855 measured ok hours earlier under a byte-identical cell script and a reader differing only by the version string. The protect opt-in itself is settled (both runs: pointer .kan/seed-id, no plaintext, the guard confirmed the plane was exercised -- a v0.12 writer reaches the keychain through protect). What flips is whether the protect-filed entry is readable by a different binary: ok once, blocked once, while v0.7.0..v0.11.0's default-filed entries measured keychain-modal in 18 of 18 jobs across both runs. Recorded as blocked because that matches the stable control, the majority of jobs, and the trusted-application-ACL model in .design/identity-at-rest.md; decided with Maxine. IF THIS ROW GOES RED WITH actual=ok, THAT IS #205'S FINGERPRINT, not a regression -- read it into the issue rather than re-running it away.
v0.12.0-beta.1	keychain-recovery	fix-route-blocked	MEASURED in run 31444789994 (tag push v0.12.0-beta.2, macos-latest), unstable exactly as the keychain row above: run 31439573855 measured ok hours earlier (unprotect ran to completion against a readable entry). When the entry is ACL-bound, `unprotect` must READ the keychain and blocks inside the condition it remedies, as v0.7.0..v0.11.0 stably measure. Recorded as blocked with the same reasoning and the same kan#205 fingerprint rule as the row above.
v0.12.0-beta.2	identity-file	ok	MEASURED in run 31447615539 (ubuntu-latest, beta.2's first execution as a historical writer -- a main dispatch excludes no tag). As predicted from the identical-binary analogy with beta.1's measured row.
v0.12.0-beta.2	seed	ok	MEASURED in run 31447615539 (ubuntu-latest, same first execution). As predicted.
v0.12.0-beta.2	keychain	keychain-modal	MEASURED in run 31546187500 (tag push v0.12.0-beta.4, macos-latest). REPINNED FROM ok, ending the deliberate opposite-side straddle with beta.1's row. The straddle was designed so that whichever side a run landed on, one pair went red and that red read into kan#205 -- but it also meant the matrix could never be green, and a permanently red gate is one nobody reads (ADR-78: v0.9.1's tag-push red went unread for two days). Blocked is now pinned because it is what the trusted-application-ACL model predicts (.design/identity-at-rest.md), what the stable control measures (v0.7.0..v0.11.0 blocked 18/18, and every one of those rows passed this run), what 3 of 5 runs of the v0.12 rows measured, and what an upgrade write against a copy of kan's own keychain-backed log did locally (blocked on the authorization prompt, killed after 90s, PR #208). The two ok measurements -- runs 31439573855 and 31447615539 -- are retained here as the anomaly kan#205 is about, not discarded. THE FINGERPRINT RULE NOW POINTS THE OTHER WAY AND IS SHARPER FOR IT: if this row goes red with actual=ok, that is #205 firing, and the red is signal rather than the background it had become.
v0.12.0-beta.2	keychain-recovery	fix-route-blocked	MEASURED in run 31546187500 (tag push v0.12.0-beta.4, macos-latest). Repinned from ok alongside the keychain row above and for the same reasons: an ACL-bound entry makes `unprotect` block inside the read it must perform to remedy, which is the stable behaviour v0.7.0..v0.11.0 show. Same #205 fingerprint rule -- a future actual=ok here is the anomaly, not a regression.
v0.12.0-beta.3	identity-file	ok	MEASURED in run 31540991285 (ubuntu-latest, PR of #208 -- beta.3's first execution as a historical writer). Converts the cut-time prediction, which held: the harness seeds the key (route 2) and the reader follows the named file. This run also exercised the new UPGRADE WRITE step for the first time -- the reader appends a claim after reading, and all of beta.3's claims survived it, so this row now covers the flat-to-canonical MST rebuild (kan#204, ADR-90) and not only the read.
v0.12.0-beta.3	seed	ok	MEASURED in run 31540991285 (ubuntu-latest, same first execution). Converts the cut-time prediction: .kan/seed is what a fresh v0.12 workspace produces and the default path reads it clean. Covers the upgrade write too -- see the identity-file row above for what that step added.
v0.12.0-beta.3	keychain	keychain-modal	MEASURED in run 31546187500 (tag push v0.12.0-beta.4, macos-latest) -- beta.3's first execution on this axis, and it CONFIRMED the cut-time prediction rather than disagreeing with it: the trusted-application-ACL model (.design/identity-at-rest.md) said a protect-filed entry is unreadable by a different binary, and it was. Converted because a prediction that has run must become a measurement. This run landed wholly on the blocked side -- all four v0.12 keychain jobs agreed, and every stable-control row v0.1.1..v0.12.0-beta.1 passed pinned blocked. Same kan#205 fingerprint rule stands: if this row ever goes red with actual=ok, that is #205, not a regression.
v0.12.0-beta.3	keychain-recovery	fix-route-blocked	MEASURED in run 31546187500 (tag push v0.12.0-beta.4, macos-latest), confirming the prediction alongside the keychain row above: an ACL-bound entry makes `unprotect` block inside the read it must perform to remedy. Same #205 fingerprint rule.
v0.12.0-beta.4	identity-file	ok	MEASURED in run 31561823997 (PR #215, ubuntu-latest) -- beta.4's FIRST execution as a historical writer, confirming the cut-time prediction exactly. It executed at all because of ADR-91: beta.4 is excluded from the matrix while HEAD builds what beta.4 builds, and this branch changes src/, so beta.4 became a genuine writer rather than a binary compared against itself. THE FIRST-CONFORMANT-WRITER CAVEAT THIS ROW RAISED IS NOW ANSWERED. beta.4 is the first version to write a conformant MST (kan#204, ADR-90), so the reader was handed a real tree instead of a flat root node and the upgrade write exercised canonical-to-canonical rather than flat-to-canonical for the first time. The row predicted that any divergence from beta.3's twin would be the MST change and should be read as such; there was none, so the MST change is invisible at the identity-at-rest plane, which is what it should be.
v0.12.0-beta.4	seed	ok	MEASURED in run 31561823997 (PR #215, ubuntu-latest), confirming the cut-time prediction alongside the identity-file row above and under the same ADR-91 reason for executing now. .kan/seed is what a fresh v0.12 workspace produces and the default path read it clean, canonical MST and all -- the first-conformant-writer caveat is answered here too.
v0.12.0-beta.4	keychain	keychain-modal	PREDICTED at cut time -- the trusted-application-ACL model (.design/identity-at-rest.md) says a protect-filed entry is unreadable by a different binary, which is the stable control (v0.7.0..v0.11.0: 18/18). Same kan#205 fingerprint rule as beta.3's row: IF THIS GOES RED WITH actual=ok, THAT IS #205, not a regression -- convert it to the measurement and read it into the issue rather than re-running it away. Independent local evidence for the blocked side: an upgrade write against a copy of kan's own keychain-backed log blocked on the authorization prompt and had to be killed (PR #208).
v0.12.0-beta.4	keychain-recovery	fix-route-blocked	PREDICTED at cut time -- same model and same #205 fingerprint rule as the keychain row above: an ACL-bound entry makes `unprotect` block inside the read it must perform to remedy.
